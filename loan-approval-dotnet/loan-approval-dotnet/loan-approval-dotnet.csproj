<Project Sdk="Microsoft.NET.Sdk.Web">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <RootNamespace>loan_approval_dotnet</RootNamespace>
  </PropertyGroup>

  <!-- Keep project file minimal. Test projects and .github docs should live at the repository root and not be included in the web project. -->
  <ItemGroup>
    <None Include="loan-approval-dotnet.sln" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Swashbuckle.AspNetCore" Version="7.0.0" />
  </ItemGroup>

</Project>
